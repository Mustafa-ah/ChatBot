<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:com="clr-namespace:STC.Common.CommonControlls;assembly=STC.Common"
             xmlns:rs="clr-namespace:STC.Resources" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="STC.Views.NewItemPage" Title="New Item">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
       <com:BasePageContentView>
           <com:BasePageContentView.AppConntentView>
                <StackLayout Spacing="20" Padding="15">
            <com:HeaderContentView Title="{Binding MyTitle}" BackgroundColor="Yellow"/>
            <Label Text="{x:Static rs:AppResources.AddButton}" FontSize="Medium" />
            <Entry Text="{Binding Item.Text}" FontSize="Small" />
            <Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />
        </StackLayout>
           </com:BasePageContentView.AppConntentView>
       </com:BasePageContentView>
    </ContentPage.Content>
</ContentPage>